apiVersion: v1
kind: Service
metadata:
  name: spring
  labels:
    app: spring
spec:
  ports:
    - name: spring
      port: 8080
      #nodePort: 30780
  #type: NodePort
  selector:
    app: spring
